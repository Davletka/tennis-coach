# URL of the FastAPI backend (embedded at build time — must be accessible from the browser)
# In local dev: http://localhost:8000
# In Docker Compose: http://localhost:8000  (browser reaches host port)
NEXT_PUBLIC_API_URL=http://localhost:8000
